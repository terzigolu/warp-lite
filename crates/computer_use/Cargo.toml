[package]
name = "computer_use"
edition = "2024"
authors.workspace = true
publish.workspace = true
license.workspace = true

# warp-lite Phase 3.x: this crate has been gutted.
# Only the public type surface required by `ai` + `app` callers is kept,
# so the workspace continues to build while every concrete capability is a
# no-op. The original platform-specific implementation (mac/linux/windows
# input + screenshot stacks) and the `use_computer` binary were removed as
# part of the AI removal pass.

[features]
test-util = []

[dependencies]
async-trait.workspace = true
pathfinder_geometry.workspace = true
serde.workspace = true
serde_with.workspace = true
