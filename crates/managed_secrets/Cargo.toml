[package]
name = "warp_managed_secrets"
version = "0.1.0"
authors.workspace = true
publish.workspace = true
license.workspace = true
edition = "2024"

# warp-lite v0.4: this crate is an inert stub. Original Tink/HPKE deps
# (tink-core, tink-proto, tink-hybrid, hpke, zeroize, rand, base64) and
# the file-temp/log helpers used by the GCP federation flow have been
# removed.

[dependencies]
anyhow.workspace = true
async-trait.workspace = true
chrono.workspace = true
serde.workspace = true
thiserror.workspace = true
warpui.workspace = true
warp_graphql.workspace = true
vec1.workspace = true
