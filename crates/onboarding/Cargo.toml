[package]
name = "onboarding"
version = "0.1.0"
edition = "2021"
authors.workspace = true
publish.workspace = true
license.workspace = true

# warp-lite v0.4 — onboarding gutted to an inert stub. See `src/lib.rs`.

[dependencies]
serde.workspace = true
warp_core = { workspace = true }
warpui.workspace = true
